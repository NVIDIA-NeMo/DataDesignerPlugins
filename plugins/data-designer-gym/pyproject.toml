# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

[project]
name = "data-designer-gym"
version = "0.1.0"
description = "Export and ingest Gym tasks with Data Designer"
requires-python = ">=3.10"
dependencies = [
    "data-designer>=0.9.1",
]
license = "Apache-2.0"
readme = "README.md"
authors = [
    {name = "NVIDIA Corporation"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
]

[project.entry-points."data_designer.plugins"]
gym = "data_designer_gym.plugin:plugin"

[project.scripts]
data-designer-gym = "data_designer_gym.cli:main"

[project.urls]
Repository = "https://github.com/NVIDIA-NeMo/DataDesignerPlugins"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/data_designer_gym"]

[tool.ruff]
extend = "../../pyproject.toml"
